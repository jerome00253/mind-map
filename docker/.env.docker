# Configuration de la base de données
DB_HOST=mysql
DB_USER=mindmap
DB_PASSWORD=mindmap_password
DB_NAME=mindmap

# JWT Secret - Sera généré automatiquement au premier démarrage

# Node Environment
NODE_ENV=production
